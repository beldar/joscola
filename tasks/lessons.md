# Lessons

- When cropping PDF-based instruction assets, verify the final generated PNG itself at full height, not only the React layout. Algorithm pages can have extra movement rows below the initially visible panel; include the full movement sequence before marking the asset done.
